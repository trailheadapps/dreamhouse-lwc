<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>CapacityLimitRelaxation</fullName>
    <inlineHelpText>Define the relaxation behavior for this limit.
            If the work capacity limit relaxation policy is set per limit in Field Service Settings:
            Empty value - no relaxation.
            0 - relaxation starts at midnight on the day of service.
            Positive integer - relaxation starts this number of hours after midnight.
            Negative integer - relaxation starts this number of hours before midnight.
            If the work capacity limit relaxation policy isn’t set per limit, this value is ignored.</inlineHelpText>
    <trackHistory>false</trackHistory>
</CustomField>
